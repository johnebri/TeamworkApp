const baseUrl = 'https://je-teamwork.herokuapp.com/api/v1';
// const cors = 'https://cors-anywhere.herokuapp.com';

export const loginUrl = `${baseUrl}/auth/signin`;

export const feedsUrl = `${baseUrl}/feed`;

export const articlesUrl = `${baseUrl}/articles`;

export const gifsUrl = `${baseUrl}/gifs`;